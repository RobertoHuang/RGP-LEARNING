# 安装Nginx镜像
```
FROM ubuntu:14.04

MAINTAINER 201210704116 "robertohuang@foxmail.com"

RUN apt-get update

RUN apt-get install -y nginx

EXPOSE 80

# 启动命令
# docker run -d --name nginx --rm -p 80:80 nginx nginx -g "daemon off;"
```